package inheritance_and_polymorphism;

import java.util.ArrayList;
import java.util.Arrays;

public class Practice {
	
	
	public static void main(String[] arge){
	//public class ICalculator2 extends ICalculator{
	// if exist a class in ICalculator i can get access by
	// calling super.div(number);
	//
	// protected String messageUponExit;
	
	
	//}
	//
	
	/*arrayList() class
	 * Declaration:
	 * ArrayList<Double> taxRates = new ArrayList<Double>(Arrays.asList(0.10, 0.15, 0.21, 0.28, 0.31));
	 * cant be int, it has to be Integer
	 * 
	 * Access:
	 * arr.get(0);
	 * x = arr.get(arr.size() - 2);
	 * 
	 * Modifications:
	 *  a.set(0,3); // sets 3 at [0]
	 */
	ArrayList<Double> a = new ArrayList<Double>(Arrays.asList(0.10, 0.15, 0.21, 0.28, 0.31));
	
	
	}
}
